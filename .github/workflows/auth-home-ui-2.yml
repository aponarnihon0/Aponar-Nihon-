name: Auth Home UI 2
on:
  push:
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 patch_auth_home.py
      - run: |
          git config user.name "MD SAMMIR"
          git config user.email "sammir160432@gmail.com"
          git add sw.js
          git commit -m "Add login profile access to home" || exit 0
          git push
