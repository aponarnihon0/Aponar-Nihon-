name: Auth Home UI 3
on:
  push:
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: |
          git pull --rebase origin main
          python3 patch_auth_home.py
          git config user.name "MD SAMMIR"
          git config user.email "sammir160432@gmail.com"
          git add sw.js
          git commit -m "Add login profile access to home v17" || exit 0
          git pull --rebase origin main
          git push
